"""
Tuple, Python'daki yerleşik veri türlerinden biridir. Python tuple'ı, parantez () içine alınmış, 
virgülle ayrılmış öğeler dizisidir.
"""

#Örnek
tup1 = ("Rohan", "Physics", 21, 69.75)
tup2 = (1, 2, 3, 4, 5)
tup3 = ("a", "b", "c", "d")
tup4 = (25.50, True, -55, 1+2j)

# * Python tuple'ın farklı veri türlerinde nesneleri olabilir.

"""
? Python tuple ve list'in her ikisi de dizidir.
? kisi arasındaki en büyük farklardan biri:
 ? Python listesinin değişken olması, buna karşın Tuple'ın değişmez olmasıdır.
 ? tuple'daki herhangi bir öğeye indeksi kullanılarak erişilemez ve 
 ? değiştirilemez, 
 ? kaldırılamaz veya eklenemez.
"""

